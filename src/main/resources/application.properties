# Server configurations
server.port=${PORT:8080}

# ==================================================
# Base de Datos - Seleccionar una opción
# ==================================================

# --- OPCIÓN 1: Supabase (PostgreSQL - Producción/Nube) ---
# spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://YOUR_SUPABASE_HOST:5432/postgres}
# spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
# spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:YOUR_SUPABASE_PASSWORD}
# spring.datasource.driver-class-name=org.postgresql.Driver
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# --- OPCIÓN 2: SQLite (Descomentar para desarrollo local rápido) ---
# spring.datasource.url=jdbc:sqlite:database.sqlite
# spring.datasource.driver-class-name=org.sqlite.JDBC
# spring.jpa.database-platform=org.hibernate.community.dialect.SQLiteDialect
# spring.jpa.properties.hibernate.connection.provider_disables_autocommit=false

# --- OPCIÓN 3: MySQL (Descomentar para usar MySQL) ---
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/smartgov}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:1234}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# JPA/Hibernate Properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Logging levels
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG

# JWT Configurations
jwt.secret=smart_gov_sync_secret_key_2026_spring_boot_super_secret
jwt.expiration=86400000
